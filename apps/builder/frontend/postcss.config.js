// PostCSS config is NOT needed for Tailwind v4 with @tailwindcss/vite plugin
// The Vite plugin handles all processing directly
// Keeping empty config to avoid PostCSS errors from other tools
export default {
  plugins: {},
}