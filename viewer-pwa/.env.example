# Mode Configuration
# By default, the viewer uses static JSON files from /public/data/
# Set to 'true' to connect to the Builder API instead
VITE_USE_API=false

# API Configuration (only used when VITE_USE_API=true)
VITE_API_URL=http://localhost:5001/api

# For production with static JSON (default)
# Just deploy the built files - no API needed!

# For production with API
# VITE_USE_API=true
# VITE_API_URL=https://boxiii.com/api